data transfer

html to ts

html = view
ts = component

ie;
          view to component

1.one by binding(one way binding)
  1.1. component to view
    1.1.1. string interpolation  {{}}
    1.1.2. property binding [property]="value" 

  1.2. view to component
    1.2.1. event binding (event name)="fuctionname()"  
    1.2.2. event binding using $event    // how to use:- (event name)="functionname($event)"
    1.2.3. event binding using template rendering variable   (#variable_name)


2.two way binding
  2.1.ngModel  FormsModule


  //redirection
-----------------
method:= navigateByUrl     (Router class method)

//dependency injection : a class in another class  (data sharing is processing between these two classes)
//the new class is add on constructer()
//access specifier:- private for a specific one (add 'private' previously to the router  eg.constructer(private router:Router))


angular services
----------------
to store redendent datas (duplicate dats)

ng g s service_name

---------------------------------------------------------------------------------------

validating inputs

*Angular form validation
      1.template driven form   (html)
      2.model driven form      (ts)                  //easy to validate (use this method) (in validator class)
            2.1-Reactive form
                -form group
                -array
                -controller

                // in regular expression 
                1.rules in [] or not in[]
                quantifiers==
                +(min 1 value & max infinite)
                *(0 too accepted)

  ----------------------------------
  LOCALSTORAGE in angular--
    -setItem()
    -getItem()
    -removeItem()

to create http request
-import HttpClientModule
    -class name HttpClient

api request is a asynchronous request to solve assynchronous request
in ts we use RxJs method ...observable ...resolve...and subscribe()
                          (js promise) ...reject...catch()